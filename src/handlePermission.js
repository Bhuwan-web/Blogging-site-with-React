const handlePermission = () => {
    const permissionForm=document.querySelector('.permission-box-form');
    // console.log(permissionForm.password.value);
    return(permissionForm.password.value);
}
 
export default handlePermission;